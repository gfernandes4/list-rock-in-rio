import 'package:flutter/material.dart';

// Definindo as cores principais
const Color primaryColor = Colors.indigo;
const Color accentColor = Colors.orange; // Exemplo de cor de destaque
const Color backgroundColor = Colors.white; // Cor de fundo, se necessário
const Color textColorTitle = Colors.white; // Cor do texto